services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: plantapp
      POSTGRES_PASSWORD: plantapp
      POSTGRES_DB: plantapp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: